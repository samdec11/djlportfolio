.row
  .twelve.columns
    %h2 Links
    - if @links.any?
      %ul
        - @links.each do |link|
          %li= link_to link.display, link.address, target: '_blank'
    - else
      .twelve.columns
        %h3 Coming soon!